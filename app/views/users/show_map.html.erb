<h4 class="text-center my-3">Explore the map - there are <%= @users.count %> users who shares the same interests as you around</h4>

<div id="map"
     style="width: 100%; height: 600px;"
     data-markers="<%= @markers.to_json %>"
     data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
</div>
